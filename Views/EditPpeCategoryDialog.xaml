<Window x:Class="PersonalPPEManager.Views.EditPpeCategoryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:PersonalPPEManager.ViewModels" mc:Ignorable="d"
        Title="{Binding WindowTitle}" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow" ResizeMode="NoResize" ShowInTaskbar="False" 
        MinWidth="350" MaxWidth="500"
        DataContextChanged="EditPpeCategoryDialog_DataContextChanged">
    <Border Padding="20">
        <StackPanel>
            <TextBlock Text="{Binding WindowTitle}" Margin="0,0,0,15" FontSize="14" FontWeight="Bold"/>

            <Grid DataContext="{Binding CurrentCategory}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" MinWidth="70"/>
                    <ColumnDefinition Width="*" MinWidth="230"/>
                </Grid.ColumnDefinitions>

                <Label Grid.Row="0" Grid.Column="0" Content="类别名称:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding CategoryName, UpdateSourceTrigger=PropertyChanged}" Margin="0,3,0,3" Padding="3" MaxLength="100"/>

                <Label Grid.Row="1" Grid.Column="0" Content="备注:" VerticalAlignment="Top" Margin="0,5,5,0"/>
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Remarks, UpdateSourceTrigger=PropertyChanged}" Margin="0,3,0,3" MinHeight="60" MaxHeight="100" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Padding="3"/>
            </Grid>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,25,0,0">
                <Button Content="保存" Command="{Binding SaveCommand}" IsDefault="True" MinWidth="80" Margin="0,0,10,0" Padding="10,5"/>
                <Button Content="取消" Command="{Binding CancelCommand}" IsCancel="True" MinWidth="80" Padding="10,5"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>