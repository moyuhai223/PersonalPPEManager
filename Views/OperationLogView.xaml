<UserControl x:Class="PersonalPPEManager.Views.OperationLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:PersonalPPEManager.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             Background="White">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="操作日志记录" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>

        <Border Grid.Row="1" BorderBrush="LightGray" BorderThickness="1" Padding="10" Margin="0,0,0,10" CornerRadius="3">
            <StackPanel Orientation="Horizontal">
                <Label Content="从日期:" VerticalAlignment="Center"/>
                <DatePicker SelectedDate="{Binding FromDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0" VerticalAlignment="Center" Width="120"/>

                <Label Content="至日期:" VerticalAlignment="Center"/>
                <DatePicker SelectedDate="{Binding ToDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,20,0" VerticalAlignment="Center" Width="120"/>

                <Label Content="操作类型包含:" VerticalAlignment="Center"/>
                <TextBox Text="{Binding OperationTypeFilter, UpdateSourceTrigger=PropertyChanged}" MinWidth="150" Margin="0,0,10,0" VerticalAlignment="Center"/>

                <Button Content="查询日志" Command="{Binding LoadLogsCommand}" Padding="10,5" Margin="0,0,5,0" VerticalAlignment="Center"/>
                <Button Content="清除筛选" Command="{Binding ClearFiltersCommand}" Padding="10,5" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding LogEntries}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  SelectionMode="Extended" 
                  SelectionUnit="FullRow"
                  VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Auto">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding LogID}" Width="60" SortDirection="Descending"/>
                <DataGridTextColumn Header="时间戳" Binding="{Binding Timestamp}" Width="160"/>
                <DataGridTextColumn Header="操作类型" Binding="{Binding OperationType}" Width="180"/>
                <DataGridTemplateColumn Header="描述" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Description}" TextWrapping="Wrap" VerticalAlignment="Center" Padding="3"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>