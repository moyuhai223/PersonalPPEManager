<UserControl x:Class="PersonalPPEManager.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:PersonalPPEManager.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="系统参数设置" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

            <StackPanel Grid.Row="1">
                <TextBlock Text="劳保用品最大持有数量上限：" Margin="0,0,0,10" FontWeight="SemiBold"/>
                <Border Padding="10" BorderBrush="LightGray" BorderThickness="1" CornerRadius="3">
                    <Grid MaxWidth="400">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto" MinWidth="80"/>
                        </Grid.ColumnDefinitions>

                        <Label Grid.Row="0" Grid.Column="0" Content="洁净服最大持有套数:" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding MaxActiveSuits, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" MinWidth="50" MaxLength="2" HorizontalContentAlignment="Right" Padding="3"/>

                        <Label Grid.Row="1" Grid.Column="0" Content="帽子最大持有顶数:" VerticalAlignment="Center" Margin="0,5,0,0"/>
                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding MaxActiveHats, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" MinWidth="50" MaxLength="2" HorizontalContentAlignment="Right" Padding="3"/>

                        <Label Grid.Row="2" Grid.Column="0" Content="白色劳保鞋最大持有双数:" VerticalAlignment="Center" Margin="0,5,0,0"/>
                        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding MaxActiveSafetyShoes, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" MinWidth="50" MaxLength="2" HorizontalContentAlignment="Right" Padding="3"/>

                        <Label Grid.Row="3" Grid.Column="0" Content="白色帆布鞋最大持有双数:" VerticalAlignment="Center" Margin="0,5,0,0"/>
                        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding MaxActiveCanvasShoes, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" MinWidth="50" MaxLength="2" HorizontalContentAlignment="Right" Padding="3"/>
                    </Grid>
                </Border>

                <StackPanel Orientation="Horizontal" Margin="0,25,0,0">
                    <Button Content="加载当前设置" Command="{Binding LoadCurrentSettingsCommand}" Padding="10,5" Margin="0,0,10,0" />
                    <Button Content="恢复默认设置" Command="{Binding RestoreDefaultsCommand}" Padding="10,5" Margin="0,0,10,0" Background="Orange"/>
                    <Button Content="保存设置" Command="{Binding SaveSettingsCommand}" Padding="10,5" FontWeight="Bold" Background="ForestGreen" Foreground="White"/>
                </StackPanel>
                <TextBlock Text="注意：部分设置（如数量上限）的更改可能需要重新进入相关功能模块或重启应用才能完全生效。" FontSize="10" FontStyle="Italic" Margin="0,15,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>